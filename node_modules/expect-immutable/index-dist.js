'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _expect = require('expect');

var _expect2 = _interopRequireDefault(_expect);

var _immutable = require('immutable');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function ensureIterable(object) {
  _expect2.default.assert(_immutable.Iterable.isIterable(object), 'Expected %s to be an Immutable Iterable', object);
}

var api = {
  toEqualImmutable: function toEqualImmutable(iterable) {
    ensureIterable(this.actual);
    ensureIterable(iterable);

    (0, _expect2.default)(this.actual.toJS()).toEqual(iterable.toJS());
  },
  toNotEqualImmutable: function toNotEqualImmutable(iterable) {
    ensureIterable(this.actual);
    ensureIterable(iterable);

    (0, _expect2.default)(this.actual.toJS()).toNotEqual(iterable.toJS());
  }
};

exports.default = api;
